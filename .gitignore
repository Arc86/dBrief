# macOS
.DS_Store

# Python build/test caches
__pycache__/

# Swift Package Manager / Xcode build output
/.build
/Packages
xcuserdata/
DerivedData/
.swiftpm/configuration/registries.json
.swiftpm/xcode/package.xcworkspace/contents.xcworkspacedata
.netrc

# Built app bundle (reproduce with `make app`; distribute via GitHub Releases)
/dBrief.app/

# Built beta channel bundle (reproduce with `make beta`; never distributed)
/dBrief-Beta.app/

# Local lock for the tracked beta build counter.
/packaging/.beta-build-number.lock

# Built DMG (reproduce with `make dmg`; upload to GitHub Releases)
/dBrief-*.dmg

# Editor / AI-assistant tooling
.vscode/
.claude/
.kilo/
.superpowers/
.worktrees/
.serena/
graphify-out/

# Internal development artifacts (not for the public repo)
# Anchored to the repo root so it does NOT match the published site/docs/.
/docs/
tasks/
MEMORY_FIXES_SUMMARY.md
DEVELOPMENT_ROADMAP.md
monitor_inference.sh
